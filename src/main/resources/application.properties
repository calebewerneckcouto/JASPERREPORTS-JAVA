spring.datasource.url=jdbc:postgresql://localhost:5432/cwcdcomb_formacao-jasper-report?autoreconnect=true
spring.datasource.username=cwcdcomb_cwc3d
spring.datasource.password=cwc3d14694899
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
spring.jmx.enabled= false

#número máximo de milissegundos que um cliente aguardará por uma conexão
spring.datasource.hikari.connection-timeout = 20000 

#número mínimo de conexões inativas mantidas pelo HikariCP em um conjunto de conexões
spring.datasource.hikari.minimum-idle= 10

#Máximo do pool de conexão
spring.datasource.hikari.maximum-pool-size= 40

#Tempo ociosos para conexão
spring.datasource.hikari.idle-timeout=10000 

#salvando dados no banco automaticamente
spring.datasource.hikari.auto-commit =true

# Configura��o do encoding UTF-8 para Spring MVC
spring.mvc.charset=UTF-8
spring.mvc.servlet.charset=UTF-8

